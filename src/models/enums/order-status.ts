export enum OrderStatus {
  Pending = "pending",
  WaitingForConfirmation = "waiting-for-confirmation",
  Declined = "declined",
  Confirmed = "confirmed",
  Shipped = "shipped",
  Delivered = "delivered",
}
